<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SCP">
        <activity android:name=".Patient.PatientMainActivity"></activity>
        <activity android:name=".Caretaker.CaretakerMainActivity" />
        <activity android:name=".Caretaker.RegestrationPatientCaretaker" />
        <activity android:name=".Admin.RegestrationPatientAdmin" />
        <activity android:name=".Admin.RegistrationCaretakerActivity" />
        <activity android:name=".Admin.AdminMainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>